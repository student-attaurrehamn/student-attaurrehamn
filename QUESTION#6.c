The x=5 > 8 ? So it’s false so it will go for else which is1=!2<5? So here first, comparison have a getter precedence that why 2<5 yes it’s true so output 1 and then check 1=!1 so it’s false so again else statement will be printed which is 30.Output is 30